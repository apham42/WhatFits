@model IEnumerable<Whatfits.Models.SecurityQuestion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Question)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Question)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SecurityQuestionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SecurityQuestionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SecurityQuestionID })
        </td>
    </tr>
}

</table>
